# API через Vite proxy в dev (относительный путь)
VITE_API_BASE_URL=/api/v1

# Порт backend для proxy (должен совпадать с start.bat BACKEND_PORT)
VITE_BACKEND_PORT=8011

# Порт dev-сервера Vite
VITE_DEV_PORT=5173
